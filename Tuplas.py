""" Son estructuras que permiten almacenar diferentes tipos de datos, además son inmutables
por lo que no permiten modificar o agregar elementos una vez se haya definido la tupla. """

tupla = (1, "hola, 3.5")
print(tupla)
print(tupla[0])

