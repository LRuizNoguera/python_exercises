"""" Tuplas son estructuras que permiten almacenar diferentes tipo de datos. Son inmutables
por lo cual no permiten modificar o agregar elementos una vez se haya definido la tupla"""

tupla = (1, "hola", 3.5)
print(tupla)
print(tupla[0])
tupla[0] = 2
